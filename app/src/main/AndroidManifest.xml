<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.allfavour">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AllFavour">
        <activity android:name=".ui.provider.ProviderActivity"></activity>
        <activity android:name=".ui.consumer.notifications.NotificationsActivity" />
        <activity android:name=".ui.consumer.myFavours.MyFavoursActivity" />
        <activity android:name=".ui.consumer.myInterests.MyInterestsActivity" />
        <activity android:name=".ui.consumer.profile.ProfileActivity" />
        <activity android:name=".ui.ChatActivity" />
        <activity android:name=".ui.consumer.ConsumerActivity" />
        <activity android:name=".ui.register.RegisterActivity" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>