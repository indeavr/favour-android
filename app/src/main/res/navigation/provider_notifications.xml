<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/provider_notifications_navigation"
    app:startDestination="@id/provider_notifications_dest">

    <fragment
        android:id="@+id/provider_notifications_dest"
        android:name="com.example.allfavour.ui.provider.notifications.NotificationsFragment"
        android:label="Notifications"
        tools:layout="@layout/provider_notifications_fragment" />

    <deepLink
        android:id="@+id/provider_notifications_deepLink"
        app:uri="www.nav.viewpager.com/{title}/{date}" />

</navigation>